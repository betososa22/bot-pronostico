name: Scheduled Pronosticos

on:
  schedule:
    - cron: "0 12 * * *"   # 07:00 America/Bogota
    - cron: "0 19 * * *"   # 14:00 America/Bogota
  workflow_dispatch: {}     # permitir correrlo manualmente

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install httpx pytz

      - name: Run once (send pronosticos)
        env:
          BOT_TOKEN: "7500570637:AAEWH2Bdw8STZGoobHfabRpy_DOwwgLjTMY"
          API_FOOTBALL_KEY: "510811e125a60b7e0caba0690fdd6874"
          SEASON_HIST: "2023"
        run: |
          python pronosticos.py
